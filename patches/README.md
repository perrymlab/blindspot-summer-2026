# Patches

This folder stores local patches for external repositories that are not committed directly into this repository.

- `0001-Add-PRIME-ReID-poison-and-export-hooks.patch`: applies the PRIME ReID poisoning/export integration to upstream BoT-SORT.

Use `python scripts/setup_repo.py` to clone BoT-SORT and apply this patch automatically.
